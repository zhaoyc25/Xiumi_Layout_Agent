# PLAN.md — 项目实施计划

本文件是唯一的路线图。每完成一个里程碑，勾选对应项并在"完成记录"里追加一行。所有 Agent 开发工作仍须遵守 [AGENTS.md](AGENTS.md)（小步改动、每步有测试、禁止手写 HTML、不擅改原文）。

## 总体思路

**先建主管，后写工具**：先把对话主管（chat supervisor）立起来——它懂完整工作流、能跟客户用大白话对话、通过统一的工具注册表调度各模块；所有工具先注册成桩（被调用时回复"这个功能还没做好，我先记下了"）。之后每个里程碑就是把某个桩换成真实现，主管与客户的交互方式不变。

客户是**不懂电脑的编辑部人员**，只做两件事：

1. 跟 Agent 说话（自然语言，TUI，未来 Web UI）；
2. 把文件（文字稿 txt/word、图片、模板 HTML）**一个一个**丢进固定文件夹 `inbox/`。

剩下的（移动归档、格式化、分级、配图、套模板、生成 result.html）全部由 Agent 完成。分级不明确的地方由 LLM 辅助判断，**判断结果必须展示给客户确认**，不静默决定。

```
                        ┌─────────── 对话主管 chat/（先建） ───────────┐
客户 ──自然语言──▶ TUI ─▶ Agent循环(LLM) ◀──▶ 工具注册表 ──▶ 工具桩→真实现
  │                     ▲   └─ 状态机：开新项目→收模板→收新稿→确认分级→替换→交付
  │                     │
  └──逐个丢文件──▶ inbox/ ──归档工具──▶ workspace/<task_id>/input/
                              normalize / template / replace / image / storage = 工具实现层
```

## 里程碑

### M0 脚手架 ✅（已完成）
- [x] 目录结构、pyproject.toml、.gitignore
- [x] AGENTS.md / README.md / SECRET.md / requirements.md

### M1 对话主管 chat/（supervisor，先建）✅（已完成）
- [x] LLM 适配层：统一 `chat(messages) -> str` 接口；凭据读 `secrets/config.env`；**必须可 mock**（离线/CI 无 Key 也能跑测试）
- [x] Agent 循环：LLM 决定"说话 or 调工具"，工具结果回灌，直到回合结束
- [x] 工具注册表：每个工具 = 名称 + 入参 schema + 桩实现（返回"功能未实现，已记录"）；后续里程碑只替换桩
- [x] 状态机（workflow）：开新项目→收模板→收新稿→确认分级→替换→交付 result.html；非法跳转要拦住并解释
- [x] 工作流 system prompt：把标准工作流、大白话要求（不打代码术语）、铁律（不改原文、不手写 HTML）写进主管人设
- [x] TUI：终端一问一答循环
- [x] 测试：mock LLM 下 Agent 循环、工具调度、状态机流转

### M2 固定收件箱 inbox/（第一个真工具）
- [x] `inbox/` 为客户唯一投递处：一次一个文件，文件名即自然语言的一部分（如 `这是新文字稿.txt`）
- [x] 扫描工具：识别文件类型（文字稿/图片/模板HTML/模板文字稿），归档移动到 `workspace/<task_id>/input/` 对应位置
- [x] 无法识别类型/归属时，主管停下来问客户，绝不乱归档
- [x] 测试：各类文件投放 → 归档位置断言

### M3 文字稿清洗与分级（normalize，替换桩）
- [x] 纯规则版：txt → 结构化文档（h1/h2/…/body/image_slot），容忍全角空格、多余空行、word 粘贴脏字符
- [x] 分级置信度：明确（规则命中）/ 不明确（需 LLM 复核）
- [x] 测试：脏输入样例 → 期望结构

### M4 LLM 辅助分级（normalize 内接 LLM）
- [x] 规则置信度低 → 调 LLM 给出分级建议 + 理由
- [x] 结果仅作建议，主管**逐条展示给客户确认/修改**后才生效；原文一字不改
- [x] 测试：mock LLM 返回 → 建议结构断言

### M5 模板解析与标准格式文件（template，替换桩）
- [x] 读模板文字稿 + 模板 HTML，产出标准格式文件（层级↔HTML 片段映射，JSON）
- [x] 层级匹配有歧义时，停下来问人工（不猜）
- [x] 测试：样例模板 → 期望 JSON

### M6 核心替换（replace，替换桩）
- [x] 结构化新稿 + 标准格式文件 → result.html
- [x] 对应层级节点过多→复制，过少→删除；只改文本节点与图片 src
- [x] 测试：节点增删/替换的对照用例

### M7 图片处理（image，替换桩）
- [x] 图片位标记（插入位置随文字稿分级产出）
- [x] 图床上传取外链（凭据读 `secrets/config.env`）
- [x] 预留 `ImageProcessor` 接口（修图/拼图，只留桩）
- [x] 测试：标记与上传逻辑（mock 网络）

### M8 模板库（storage，替换桩）
- [x] 模板入库/取用/选用对话（"用上次会议那个模板"）
- [x] templates/ 目录元数据（名称、适用场景、层级结构摘要）
- [x] 测试：入库→检索→选用

### M9 修图/拼图（image.ImageProcessor）
- [x] 接口落地：拼图、裁剪 3:2 / 1:1 等编辑部常用比例
- [x] 对话触发："把这三张拼成一张"
- [x] 测试：本地图片处理用例

### M10 Web UI（远期）
- [ ] 对话框 + inbox/ 拖拽上传 + result.html 预览
- [ ] 后端复用 chat 主管与工具层，不重写业务逻辑

## 关键设计决定

| 决定 | 内容 | 理由 |
| --- | --- | --- |
| 开发顺序 | **先建主管（M1），工具全部挂桩，之后逐个填实现** | 主管+注册表定型后，后续每个里程碑边界清晰、可独立测试；客户从第一天起就能对话 |
| 语言 | Python 3.11+ | 见 README 技术选型 |
| 交互形态 | 先 TUI 后 Web UI，业务逻辑与交互层分离（chat 模块不含排版逻辑） | 客户不懂电脑，交互层会反复改，逻辑层必须稳定 |
| 文件入口 | 唯一固定 `inbox/`，逐个投递 | 最简单的客户操作；Agent 负责归档，避免客户理解目录结构 |
| LLM 用途 | 只做"理解自然语言 + 调度 + 分级建议"，不做 HTML 生成 | 铁律：HTML 只能来自模板节点替换/复制/删除，LLM 不可靠 |
| LLM 可 mock | chat 适配层必须支持无 Key 离线运行 | CI/测试不依赖外网与费用 |
| 分级兜底 | 规则先行，置信度低才调 LLM，且必须客户确认 | 省钱、可控、责任在人 |
| 敏感信息 | 一律 `secrets/`，见 SECRET.md | 铁律 |

## 完成记录

| 日期 | 里程碑 | 摘要 |
| --- | --- | --- |
| 2026-08-30 | M0 | 脚手架 + 三份规范文档 |
| 2026-08-30 | PLAN 重排 | 按用户决定改为"先建主管后写工具"，M1=chat 主管，工具挂桩 |
| 2026-08-30 | M1 | chat/ 主管完成：[OI] 兼容 LLM 适配层（可 mock）、Agent 循环（CALL/SPEAK 协议）、8 工具注册表（7 桩）、6 阶段状态机、TUI；pytest 24 passed / ruff clean |
