# PLAN.md — 项目实施计划

本文件是唯一的路线图。每完成一个里程碑，勾选对应项并在"完成记录"里追加一行。所有 Agent 开发工作仍须遵守 [AGENTS.md](AGENTS.md)（小步改动、每步有测试、禁止手写 HTML、不擅改原文）。

## 总体思路

**先建主管，后写工具**：先把对话主管（chat supervisor）立起来——它懂完整工作流、能跟客户用大白话对话、通过统一的工具注册表调度各模块；所有工具先注册成桩（被调用时如实回复"现在还没有这个工具"）。之后每个里程碑就是把某个桩换成真实现，主管与客户的交互方式不变。

**固定引导优先，LLM 兜底**：收材料阶段的问答（"请把…放进 inbox，放好后输入 y"）全部由固定程序完成，零 LLM 调用；材料收齐归档后才让 LLM 接手，按三阶段干活（检查材料 → 分级展示 → 生成成品）。

客户是**不懂电脑的编辑部人员**，只做两件事：

1. 跟 Agent 说话（自然语言，TUI，未来 Web UI）；
2. 把文件（文字稿 txt/word、图片、模板 HTML）**一个一个**丢进固定文件夹 `inbox/`。

```
客户 ──y/没有──▶ 固定引导(guide.py, 零LLM) ──归档──▶ workspace/<task_id>/input/
                                                            │ template/ draft/ images/
                                                    材料齐后交棒 ▼
                 Agent循环(LLM) ◀──▶ 工具注册表（桩→真实现）
                 └─ 三阶段：①检查材料 ②分级展示(outbox/) ③生成成品(outbox/)
```

## 里程碑

### M0 脚手架 ✅（已完成）
- [x] 目录结构、pyproject.toml、.gitignore
- [x] AGENTS.md / README.md / SECRET.md / requirements.md

### M1 对话主管 chat/（supervisor，先建）✅（已完成）
- [x] LLM 适配层：统一 `chat(messages) -> str` 接口；凭据读 `secrets/config.env`；**必须可 mock**（离线/CI 无 Key 也能跑测试）
- [x] Agent 循环：LLM 决定"说话 or 调工具"，工具结果回灌，直到回合结束；兼容 `CALL name {json}` 自定义协议与 LLM 原生 `<tool_call>{json}</tool_call>` 格式
- [x] 工具注册表：每个工具 = 名称 + 入参 schema + 桩实现（如实回复"现在还没有这个工具"）；后续里程碑只替换桩
- [x] 状态机（workflow）：开新项目→收模板→收新稿→确认分级→替换→交付；非法跳转要拦住并解释
- [x] 工作流 system prompt：大白话（不打代码术语）、三阶段流程（检查材料→分级展示→生成成品）、铁律（不改原文、不手写 HTML）
- [x] TUI：开场白"按 y 开始新项目"；收材料阶段走固定引导（零 LLM）；思考中/归档位置/进度提示
- [x] 测试：mock LLM 下 Agent 循环、工具调度、状态机流转、双格式解析

### M2 固定收件箱 inbox/ + 归档 ✅（已完成，程序接管）
- [x] `inbox/` 为客户唯一投递处，一次一个文件，格式不限（txt/doc/docx…类型判断留给 LLM）
- [x] 固定问答引导（guide.py）："请把【…】放进 inbox 文件夹，放好后输入 y"；"没有"可跳过可选项（图片）
- [x] 归档：按类别分目录 `workspace/<task_id>/input/{template,draft,images}/`，LLM 靠目录名区分模板与新稿
- [x] 垃圾文件过滤：`xxx:Zone.Identifier`、`.DS_Store`、`Thumbs.db` 等自动忽略并清掉
- [x] `xiumi clean` 指令：一键清空 inbox/ 与 workspace/ 任务文件（不经 LLM）
- [x] 测试：引导流转、归档位置、垃圾过滤

### M3 材料检查工具 scan_inbox（替换桩）✅（已完成）
- [x] 真实现：读取 `workspace/<task_id>/input/` 各目录文件清单+内容摘要，报告缺什么/什么放错了
- [x] 模板稿与新稿错位时（引导阶段先来后到对号入座的已知缺陷），靠内容特征纠正或问客户
- [x] 测试：缺文件/错位/齐全 三类用例

### M4 模板结构提取 + LLM 新稿分级映射（normalize + template，替换桩）

分工原则：**BeautifulSoup 做精确 HTML 操作，LLM 做语义理解判断。**

- [ ] **模板结构提取**（BeautifulSoup，`template/` 模块，零 LLM）：
  - 解析模板 HTML，提取每个块的"嵌入结构签名"（外层2层 section 的 display/width/color/background/text-align/border-radius）
  - 按签名分组 → 层级（同签名 = 同层级；同字号不同结构 = 不同层级，已在两个真实模板验证）
  - 区分**标题层**（独立顶层块）与**正文内子层**（嵌在 body 块内的节点）——供映射用
  - 每个层级产出：字号、颜色、**HTML 片段样例**（M7 克隆的格式模板）、内容样例
  - 产出标准格式文件（层级↔HTML 片段映射，JSON）
- [ ] **新稿清洗 + 分级 + 映射**（LLM，`normalize/` 模块）：
  - LLM 同时看到：模板结构信息（有几层、每层什么格式什么内容）+ 新稿原文
  - LLM 做三件事：① 清洗新稿（去脏字符、规范空行）② 分级（哪些是标题/正文/落款）③ 映射（新稿每块对应模板的哪个层级号）
  - 输出：`[{level: 模板层级号, text: 新稿文字}, ...]`
  - normalize 模块内部直调 `llm.chat()`，用专门 prompt 让 LLM 返回 JSON，自己解析
  - 文字清洗的机械部分（全角空格→半角、\r\n→\n）用规则预处理，LLM 做语义部分
- [ ] **展示 + 客户确认**（LLM 对话）：
  - LLM 把映射结果逐条念给客户，客户确认/修改后定稿
  - 写展示文件到 `outbox/`（缩进式纯文本大纲 + JSON），模板稿、新稿各一份
- [ ] **工具接线**：`build_template_map`（模板提取，固定Python）、`normalize_draft`（新稿清洗+分级+映射，LLM）、`review_levels`（展示确认，LLM对话）替换桩
- [ ] 测试：两个真实模板 → 期望层级结构；mock LLM 返回 → 期望分级映射

### ~~M5 LLM 辅助分级~~ → 合并进 M4
LLM 新稿分级+映射即 M4 第三步，不再单列里程碑。

### ~~M6 模板解析与标准格式文件~~ → 合并进 M4
模板 HTML 结构提取由 BeautifulSoup 完成（M4 第一步）；标准格式文件作为 M4 产出，供 M7 使用。

### M7 核心替换（replace，替换桩）

M4 产出标准格式文件 + 确认后的新稿分级映射，M7 按映射机械执行克隆+替换。

层级映射规则（在 M4 由 LLM 完成，M7 只执行）：
- **标题层**（非正文的独立块）：新稿从高到低一一对应模板从高到低，多出的不用
- **正文层**（正文块内部的子分级）：最低级保证对应；模板多出的中间级不用；少的用字号/加粗补
- M4 提取时需区分"标题层"与"正文内子层"（独立顶层块 vs 嵌在 body 内的节点）

图片处理：
- 模板正文块内部的图片节点 → **删除**
- 模板头图等非正文区域的图片 → **保持不动**
- 新稿目前只有纯文字，不插图片
- 图片插入接口预留（客户在稿子中标位置 → 对应插入），当前不实现

- [ ] 按 M4 映射关系克隆模板节点、替换文字、清理 body 内部图片/h3、拼装成 result.html
- [ ] 全程固定 Python（BeautifulSoup），零 LLM（映射已在 M4 完成）
- [ ] 成品放 `outbox/`
- [ ] 测试：节点增删/替换的对照用例（已用真实模板验证可行性）

### M8 图片处理（image，替换桩）
- [ ] 图片插入位置：由客户在稿子中对应位置标记（只标正文中间的图片，头图不管）
- [ ] 图床上传取外链（凭据读 `secrets/config.env`）
- [ ] 预留 `ImageProcessor` 接口（修图/拼图，只留桩）
- [ ] 测试：标记与上传逻辑（mock 网络）

### M9 模板库（storage，替换桩）
- [ ] 模板入库/取用/选用对话（"用上次会议那个模板"）
- [ ] templates/ 目录元数据（名称、适用场景、层级结构摘要）
- [ ] 测试：入库→检索→选用

### M10 修图/拼图（image.ImageProcessor）
- [ ] 接口落地：拼图、裁剪 3:2 / 1:1 等编辑部常用比例
- [ ] 对话触发："把这三张拼成一张"
- [ ] 测试：本地图片处理用例

### M11 Web UI（远期）
- [ ] 对话框 + inbox/ 拖拽上传 + outbox/ 成品预览
- [ ] 后端复用 chat 主管与工具层，不重写业务逻辑

## 关键设计决定

| 决定 | 内容 | 理由 |
| --- | --- | --- |
| 开发顺序 | **先建主管（M1），工具全部挂桩，之后逐个填实现** | 主管+注册表定型后，后续每个里程碑边界清晰、可独立测试；客户从第一天起就能对话 |
| 交互分工 | **固定问答收材料（零 LLM），LLM 只在材料齐后接手** | 收材料是机械流程，程序管最省钱最稳；LLM 花在检查/分级/交流上 |
| 语言 | Python 3.11+ | 见 README 技术选型 |
| 交互形态 | 先 TUI 后 Web UI，业务逻辑与交互层分离（chat 模块不含排版逻辑） | 客户不懂电脑，交互层会反复改，逻辑层必须稳定 |
| 文件入口 | 唯一固定 `inbox/` 逐个投递，产出统一放 `outbox/` | 最简单的客户操作；Agent 负责归档，避免客户理解目录结构 |
| 归档结构 | `input/{template,draft,images}/` 按类别分目录 | LLM 靠目录名区分材料，不猜文件名 |
| LLM 用途 | ①对话主管（聊天/调度/念分级请人确认）②新稿清洗+分级+映射模板层级；不做 HTML 结构提取也不做 HTML 生成 | 铁律：HTML 只能来自模板节点替换/复制/删除；模板结构由 BeautifulSoup 确定性提取，HTML 操作由固定代码执行，LLM 不可靠 |
| LLM 可 mock | chat 适配层必须支持无 Key 离线运行 | CI/测试不依赖外网与费用 |
| 分工原则 | BeautifulSoup 做精确 HTML 操作（提取/克隆/替换）；LLM 做语义理解（清洗/分级/映射/对话） | 各取所长：HTML 操作要确定可复现，语义理解要灵活判断 |
| 敏感信息 | 一律 `secrets/`，见 SECRET.md | 铁律 |

## 已知问题（待后续里程碑解决）

1. **引导阶段错位**：固定引导按"先来后到"对号入座，客户一次混投多个文件可能把新文字稿收进 template/ —— 留给 M3 scan_inbox 真实现靠内容纠错。
2. LLM 偶发不按协议输出工具调用（已兼容 `<tool_call>` 原生格式，残余标签会剥掉）。
3. **LLM 处理长文本超时**：GLM-5.2 处理 200 行 / 16KB 新稿时反复超时（504 网关超时或响应过慢）。即使分块 20-50 行仍不稳定。
   - **临时方案**：要求新稿以 **Markdown 格式**提交（`#`/`##`/`###` 标注层级），用固定程序解析 Markdown 大纲（不调 LLM），只把大纲（标题级别+短预览，~1KB）交给 LLM 做模板层级映射。LLM 输入从 16KB 降到 ~1KB，输出只有层级号。
   - **待解决**：Markdown 大纲解析（固定程序）→ LLM 映射（小输入小输出）→ 拼回全文。后续考虑支持纯 txt（无 Markdown 标记）的兜底方案。

## 完成记录

| 日期 | 里程碑 | 摘要 |
| --- | --- | --- |
| 2026-08-30 | M0 | 脚手架 + 三份规范文档 |
| 2026-08-30 | PLAN 重排 | 按用户决定改为"先建主管后写工具"，M1=chat 主管，工具挂桩 |
| 2026-08-30 | M1 | chat/ 主管完成：[OI] 兼容 LLM 适配层（可 mock）、Agent 循环（CALL/SPEAK + 原生 tool_call 双协议）、9 工具注册表（7 桩+new_project+reset_all）、6 阶段状态机、TUI |
| 2026-08-30 | M2 | inbox/ 固定引导收材料（零 LLM）、按类别归档到 input/{template,draft,images}/、垃圾文件过滤、xiumi clean 指令、三阶段 prompt（检查材料→分级展示→生成成品）、outbox/ 约定；pytest 46 passed / ruff clean |
| 2026-08-31 | M3 | scan_inbox 真实现：chat/scan.py 按内容特征分类（html 标签/图片魔数/纯文本）、缺件与错位检测（template 文本偏多、网页文件/图片错目录）、报告渲染；tools.py 接线；pytest 64 passed / ruff clean |
| 2026-08-31 | M4 设计敲定 | 重大调整：模板结构提取全由 BeautifulSoup 完成（按嵌入结构签名分组，非仅字号），零 LLM；M5/M6 合并进 M4 删除；新稿清洗+分级+映射交 LLM（语义理解）；M7 替换全固定 Python（克隆+换文字）；LLM 仅用于对话+新稿语义处理。两个真实模板已验证 |
| 2026-08-31 | M4 主体完成 | template/extract.py（BeautifulSoup嵌入签名分组→层级，区分标题/正文，HTML片段样例）；normalize/clean.py（规则清洗）；normalize/level.py（LLM分级+映射，直调llm.chat返回JSON）；tools.py 接线 build_template_map+normalize_draft；tui.py 传 llm；87 passed / ruff clean |
| 2026-08-31 | 已知问题 | LLM 处理长文本（200行/16KB）反复超时；临时方案：要求新稿以 Markdown 提交，固定程序解析大纲，只把大纲交给 LLM 映射模板层级（输入从16KB降到~1KB） |
